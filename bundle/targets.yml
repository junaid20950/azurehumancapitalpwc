targets:
  dev:
    default: true
    mode: development
    workspace:
      host: https://adb-7405606441244433.13.azuredatabricks.net
      root_path: /Workspace/Repos/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
      file_path: /Workspace/Repos/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}/files

  prod:
    mode: production
    workspace:
      host: https://adb-7405616149222315.15.azuredatabricks.net
      root_path: /Workspace/Repos/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
      file_path: /Workspace/Repos/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}/files
